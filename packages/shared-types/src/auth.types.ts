// Responsibility:
// Defines shared types for authentication: LoginRequest, SessionUser, Role enum (developer, admin, academic_advisor).
//
// Layer: Shared Package - Types
//
// Communication:
// Mirrors apps/api/app/schemas/auth.py. Used by apps/web features/auth and shared/guards/RoleGuard.tsx.

export {};

