// Shared ESLint preset.
// Extended by apps/web and apps/desktop eslint configs to enforce
// consistent TypeScript/React linting rules across the monorepo.
module.exports = {
  extends: [
    "eslint:recommended"
  ]
};

