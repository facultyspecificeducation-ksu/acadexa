// Responsibility:
// Vite configuration for the React renderer: plugins (React, MUI), dev server port, and build output directory consumed by apps/desktop.
//
// Layer: Renderer - Build Configuration
//
// Communication:
// Build output (dist/) is copied into apps/desktop by scripts/build-all.sh for packaging via electron-builder.

export {};

