// Responsibility:
// Renders the login form (email/username + password) using React Hook Form, validated with schemas/login.schema.ts (Zod).
//
// Layer: Renderer - Feature: auth / Components
//
// Communication:
// On submit, calls hooks/useLogin.ts, which calls api/auth.api.ts and updates store/auth.store.ts on success.

export {};

