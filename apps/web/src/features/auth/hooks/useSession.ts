// Responsibility:
// Hook exposing the current session (user, role, expiry) from store/auth.store.ts and helpers to check role-based permissions.
//
// Layer: Renderer - Feature: auth / Hooks
//
// Communication:
// Used by app/providers/AuthProvider.tsx and shared/guards/RoleGuard.tsx for RBAC checks.

export {};

