// Responsibility:
// React Query mutation hook sending chat messages plus current context (student id / recommendation id) to api/ai-assistant.api.ts and appending responses to store/ai-assistant.store.ts.
//
// Layer: Renderer - Feature: ai-assistant / Hooks
//
// Communication:
// Used by ChatWindow.tsx. The backend explicitly scopes context so the AI cannot invent new academic decisions.

export {};

