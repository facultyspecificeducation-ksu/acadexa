// Responsibility:
// React Query hook fetching a single student's full profile (info, grades, GPA, recommendations) from api/students.api.ts.
//
// Layer: Renderer - Feature: students / Hooks
//
// Communication:
// Used by components/StudentProfile.tsx and GradeHistory.tsx. Calls FastAPI GET /api/v1/students/{id}.

export {};

