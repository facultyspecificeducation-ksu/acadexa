// Responsibility:
// Typed REST calls for student data: listStudents(filters), getStudent(id), updateStudent(id, data).
//
// Layer: Renderer - Feature: students / API Client
//
// Communication:
// Uses shared/lib/apiClient.ts to call apps/api/app/api/v1/endpoints/students.py. DTOs come from @acadexa/shared-types/student.types.ts.

export {};

