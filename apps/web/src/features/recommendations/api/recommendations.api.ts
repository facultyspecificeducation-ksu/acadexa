// Responsibility:
// Typed REST calls: getRecommendations(studentId), runEvaluation(studentId), explainRecommendation(id).
//
// Layer: Renderer - Feature: recommendations / API Client
//
// Communication:
// Uses shared/lib/apiClient.ts. DTOs come from @acadexa/shared-types/recommendation.types.ts.

export {};

