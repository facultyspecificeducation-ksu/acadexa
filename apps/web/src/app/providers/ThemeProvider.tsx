// Responsibility:
// Provides the MUI theme (theme/theme.ts) and switches direction (RTL for Arabic, LTR for English) using theme/rtl.ts based on the active i18n language.
//
// Layer: Renderer - App Shell / Providers
//
// Communication:
// Wraps App.tsx. Consumed by every feature component via MUI's theme context.

export {};

