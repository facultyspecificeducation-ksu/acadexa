// Responsibility:
// Defines the React Router route tree, mapping routes/routes.config.ts entries to feature pages wrapped in the correct layout (Dashboard/Auth/Print).
//
// Layer: Renderer - App Shell / Routing
//
// Communication:
// Reads role-based route config from routes/routes.config.ts and wraps protected routes with shared/guards/RoleGuard.tsx.

export {};

