// Responsibility:
// Root application component. Wraps the app in ThemeProvider, AuthProvider, QueryProvider and renders AppRouter.
//
// Layer: Renderer - App Shell
//
// Communication:
// Top-level component mounted by main.tsx. Provides context consumed by every feature module.

export {};

