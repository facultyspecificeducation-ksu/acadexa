// Responsibility:
// Defines shared TypeScript types used across the Electron main process, preload bridge and renderer (file dialog results, notification payloads, update status).
//
// Layer: Shared (Main / Preload / Renderer)
//
// Communication:
// Imported by preload/api/*, main/ipc/handlers/*, and apps/web shared/lib/electronBridge.ts.

export {};

