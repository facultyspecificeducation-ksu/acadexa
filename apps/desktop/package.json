{
  "_meta": "Electron shell package. Depends on @acadexa/shared-types for IPC payload typing and is responsible only for OS-level concerns; all business logic lives in apps/web and apps/api.",
  "name": "@acadexa/desktop",
  "version": "0.1.0",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "dist": "electron-builder"
  }
}

