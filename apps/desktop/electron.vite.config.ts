// Responsibility:
// Vite configuration for building the Electron main, preload and renderer bundles (electron-vite). Defines entry points and output directories.
//
// Layer: Electron Build Configuration
//
// Communication:
// Used by the build pipeline (scripts/build-all.sh) to produce dist/ output consumed by electron-builder.yml.

export {};

